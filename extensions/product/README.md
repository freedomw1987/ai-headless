# Product Extension

> Auto-generated by `/extension create product` (Sprint 55)

## 簡介

Product 是一個 CRUD extension，提供基本的新增、查詢、更新、刪除功能。

## 欄位

| `name` | string | Name |
| `price` | number | Price |
| `stock` | number | Stock |
| `isActive` | boolean | Is Active |
| `createdAt` | datetime | 建立時間 |
| `updatedAt` | datetime | 更新時間 |

## Hooks

- `product.beforeCreate` — 自動設定 createdAt / updatedAt

## Actions

- `product.complete` — 切換 completed 狀態

## Computed

- (無，無 dueDate 欄位)

## Workflow

- draft → published 狀態機

## API

- `GET /api/crud/product` — 列出所有
- `POST /api/crud/product` — 新增
- `GET /api/crud/product/[id]` — 取得單筆
- `PATCH /api/crud/product/[id]` — 更新
- `DELETE /api/crud/product/[id]` — 刪除
